namespace AndBurn.HDT.Plugins.StatsConverter
{
    public enum StatsRegion
    {
        // add All for filtering regions
        All = -1,
        // use same values as Region enum, for comparison
        UNKNOWN = 0,
        US = 1,
        EU = 2,
        ASIA = 3
    }
}
